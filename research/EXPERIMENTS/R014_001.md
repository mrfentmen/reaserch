# EXPERIMENT R014_001 — reference implementation validation

- TICKET: R014 (Part 1). CODE: EXPERIMENTS/R014_001_reference.py (binary root, Euclid gcd, direct-D, no filters).
- VALIDATION: the reference implementation's outputs are corroborated everywhere checkable —
  on the R014 equivalence domain it reported the 3 known |off|=1 hits (incl. both C21-vulnerable ones the optimizer dropped);
  its root/classify logic belongs to the binary+Euclid family verified across R004/R011/R013 (dual-method agreement, Bun reconstructions).
  No reference-side bug was found; the R014_003 mismatch is attributed to pruner semantics (see R014_002), not to reference error.
- STATUS: reference logic sound (supporting artifact for R015, which reuses it).
