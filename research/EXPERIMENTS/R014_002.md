# EXPERIMENT R014_002 — pruning validation: FAILED (pruners census-unsafe)

- TICKET: R014 (Parts 2–5). CODE: EXPERIMENTS/R014_002_optimized.py + R014_003_equiv.py (exit=1, assertion fired as designed).
- DOMAIN: bases 2..60, exps 3..6 (1770 base pairs).
- RESULT: 2 FALSE REJECTIONS — (3^6+10^3, 9^3+10^3 vs 12^3, off +1) dropped by SKIP21 (gcd(6,3,3)=gcd(3,3,3)=3>2).
  Root disagreements 0, class disagreements 0 — the ONLY divergence is pruner overreach.
- C21 REFUTATION (for census use): measured — C21 removes genuine |off|=1 cases whenever gcd(xa,xb,e)>2 (witnesses above).
  FLT justifies skipping exact-equality checks only; nearness census asks a different question. C21 stays PROVEN for exact search, FORBIDDEN for census.
- C20 REFUTATION (for census use): proven by residue analysis — S=C^e±1 ranges over non-residues routinely
  (e.g. e=4/mod-16: C-odd +1 gives S≡2∉{0,1}; C-even −1 gives S≡15∉{0,1}; such (pair,e) evals are skipped
  unconditionally). No trigger on this domain, but safety is unprovable by construction: the filter answers
  "is S an e-th power" while census asks "is S within ±1 of one" — wrong question, hence unsafe regardless of hit rate.
- CRITIC NOTE: this is the gate working as designed — a premise flaw (exact-search pruners misapplied to nearness
  census) caught on the 2.2 s validation domain instead of after a 20-minute tranche. The R002 safety result
  (24=24) is UNAFFECTED: it tested exact-equality detection, the pruners' native question.
- STATUS: pruning-for-census REFUTED (C29); R014 tranche plan dead — see R014_004.
