# EXPERIMENT R008_001 — Erdos/gap hunter provenance search (researcher + critic pass)

- EXPERIMENT ID: R008_001
- TICKET: R008
- DATE: 2026-09-18 (UTC)
- QUESTION: Can the missing Erdos (and gap) hunter source be recovered without guessing?
- HYPOTHESIS (neutral): source may exist outside the obvious filenames; search history, objects, scripts, Desktop, shell history, whole-system filenames, applet bundle.
- CODE: none created (search-only; commands below, all read-only except nothing written)
- COMMANDS (workdir /Users/dtaxk):
  `git -C reaserch log --all --oneline` → 1 commit (ce27494); `branch -a` → main + origin/main only;
  `stash list` → empty; `remote -v` → origin only; `ls-remote origin` → same commit, main only;
  `git verify-pack` → pack ok (64 objects); `git fsck --lost-found` → no dangling output;
  `grep -rn selftest` (ts/js/sh/py) → only riemann.ts; `grep -rni erdos` (excl logs) → only our own audit docs;
  frontier values (1159620/1000024/1000924/done=25) → only logs + our docs;
  Desktop grep (hunt|erdos|gap|prize|research) → 2 unrelated md files;
  bash_history grep → nothing; `find / -maxdepth 4 -name *erdos*` (excl reaserch) → nothing;
  `osadecompile .../main.scpt` → `do shell script "/bin/bash \"/Users/dtaxk/Desktop/research prize/relaunch.sh\""` (no hunter paths);
  `stat` on logs → clone-time timestamps only (no provenance).
- INPUTS: repo at ce27494 + live filesystem listed above
- ACTUAL OUTPUT: no Erdos/gap hunter source found in any searched location. Single-commit history means no deleted-file recovery is possible. Log forensics (own analysis, corrected mid-pass after a dropped-final-segment bug): erdos.log = 6385 `scan n done` lines + 5×`selftest ok` + 1 nohup line; 5 runs (30/208/702/724/4721 scans; n 1000024→1159620 step 25; per-run `done` resets to 25; resume gaps 24–25); frontier matches erdos.txt=1159620. Scan lines carry zero mathematical content (no predicate output, no finds) — they assert nothing checkable even if the predicate were known. gap.log: 2^24-stepped p-scan from 9e15 with finds=0/bestmerit only.
- RUNTIME: minutes (search-bound, not compute-bound)
- COVERAGE: git objects/refs/remotes/pack (complete); repo filenames + content patterns (complete for listed patterns); Desktop/shell-history/system-filename/backup-app avenues (as listed; Time Machine volume not present/mounted — not searched, stated limitation)
- VALIDATION (critic pass): re-ran segment analysis after catching own dropped-segment bug (first pass reported 4 segments/1664 scans; corrected to 5/6385). Independent check: `grep -c` line-type census cross-checked the segmentation totals (6385+5+1+1=6392 raw lines).
- FAILURE CHECK: Time Machine / off-machine backups not searchable from here — recorded as the only unsearched avenue; everything locally searchable was searched.
- INTERPRETATION: source UNRECOVERABLE from all legitimate local avenues. No reconstruction attempted (forbidden). Erdos claim C13 stays UNVERIFIED. New repo-fact C17 (absence from searched locations) recorded as reproducible evidence.
- STATUS: ticket outcome FAILED (failure condition met: source cannot be recovered); failure itself is the evidence.
- NEXT ACTION: R007 (prime characterization).
