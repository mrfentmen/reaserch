# EXPERIMENT R017_006 — optimized tower rerun (Newton + precompute + gates)

- CODE: EXPERIMENTS/R017_006_tower_opt.py. Math IDENTICAL to baseline (critic-reviewed diff:
  root algorithm + S_A=1<<(X*X) once per X + B-power table once globally + progress prints;
  domain/classification/ties/signs/coprime-rule untouched; NO pruners; no floats).
- GATE (required before rerun): 430,360 (v,e) cases — exhaustive 1..50000, 400 random large
  (64–1200 bit), perfect-power boundaries ±2 (bases to 10^6), tower-shape 2^(X²)±2, exps 3..9 —
  Newton vs binary ZERO mismatches, defining inequalities hold everywhere.
  ARTIFACTS/R017_006_gate.txt. VERDICT: PASS.
- BENCHMARK (same inputs): binary 6.4 s / 389,545 big-pows vs Newton 0.2 s / 32,945 big-pows
  over 2261 roots → 32× wall, 11.8× fewer pows (first run); clean rerun after a cosmetic
  dispatch-bug fix: 8.8 s vs 0.4 s → ~22× wall, identical pow counts (deterministic).
  Reported speedup: ~22–35× wall (run variance), 11.8× big-pow ratio (stable).
  ARTIFACTS/R017_006_bench.txt (final clean run on file).
  Bun independent Newton spot-checks confirm inequalities. VERDICT: PASS (meaningful).
- COVERAGE (unchanged): X 8..120, odds 3..59, EXPS 3..9, both tower directions, (A,B)=1
  (automatic here: 2^X vs odd — noted, not a bug).
- TOWER RESULT: 45,878 pairs (closed form 113×406 EXACT) → |off|=1 hits: 0 (all classes).
  251 s with per-X heartbeats; sampled in-run inequality asserts silent; exit=0.
  ARTIFACTS/R017_006_stdout.txt + R017_006_counts.json (empty hit list preserved).
- INTERPRETATION: zero is null-consistent at height (Pillai-type scarcity: random-hit
  probability per trial ~ e·S^{1/e−1} ≈ 0 over 45k trials) — this neither confirms U nor
  weakens prior small-scale evidence; it EXTENDS unfalsified scope into a qualitatively new
  (tall-exponent) regime. A class-C ±1 here would have killed U; none appeared.
- CRITIC: init-bound argument checked; vacuous-prefilter noted; tie/class/sign logic
  diffed identical; L2 section intentionally not re-run (already complete in partial run).
- VERIFIER: gate + benchmark + Bun spots + closed-form pair count + monotone heartbeats
  rechecked; veto retained, no veto cast.
- PROVENANCE NOTE (2026-09-18, this cycle): the refactor order was executed a second time
  end-to-end (fresh implementation + gate + bench + tower); every number reproduced exactly
  (430,360 gate cases; 389,545/32,945 pow counts; 45,878 pairs; 0 hits). The two derivations
  were developed from the same written order, so this is re-derivation agreement, not full
  independence — recorded as corroboration, not as a second independent verification.
- STATUS: VERIFIED COMPUTATIONALLY (tower-regime zero). C36.
