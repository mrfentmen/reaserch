# R013 FORMAL CLAIM (falsification target — universal reading of the phenomenon)

## Proposition U (tested, NOT asserted)
For ALL integers A,B ≥ 2, xa,xb,e ≥ 3 with gcd(A,B)=1: let S=A^xa+B^xb,
f=⌊S^{1/e}⌋, low=S−f^e, high=(f+1)^e−S, d=low iff low<high else −high,
C=f iff d≥0 else f+1. IF |d|=1 (i.e. S−C^e=±1) THEN gcd(A,B,C)=1 AND
(gcd(A,C)>1 OR gcd(B,C)>1). [class P; T impossible here since gcd(A,B)=1.]

## Explicit assumptions
bases ≥ 2 (A=1 excluded from claim domain); all exponents ≥ 3 (exp 2 excluded);
gcd(A,B)=1 (prefilter is CLAIM SCOPE, not bias — non-coprime (A,B) makes P
automatic via that pair; tested separately in control tranche); C = nearest
e-th power to S (ties up); d signed as above; NO S-magnitude, ratio, block, or
exp-cap restrictions (all hunter artifacts dropped).

## CLAIM DOMAIN (mathematical): all tuples satisfying assumptions (infinite).
## ACTUAL SEARCH DOMAIN (this ticket): Stages A–C below (finite, stated exactly
in the experiment record). These are NEVER confused: a clean run upgrades
nothing beyond VERIFIED COMPUTATIONALLY on the stated domain; C19 stays
UNVERIFIED unless a counterexample (→ FALSE) appears.
